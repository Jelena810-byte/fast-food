<!DOCTYPE html>
<html lang="en">
<head>
  <meta charset="UTF-8">
  <title>Edit Dish</title>
  <link rel="stylesheet" href="/stylesheet/style.css">
</head>
<body>
  <%- include('../partials/_navbar.ejs') %>

  <main class="form-container">
    <h1>Edit Dish</h1>

    <form action="/users/<%= userId %>/menu/<%= menuItem._id %>?_method=PUT" method="POST">
      <label for="name">Dish Name:</label>
      <input type="text" name="name" value="<%= menuItem.name %>" required>

      <label for="price">Price ($):</label>
      <input type="number" name="price" step="0.01" value="<%= menuItem.price %>" required>

      <label for="photo">Photo URL:</label>
      <input type="text" name="photo" value="<%= menuItem.photo %>">

      <button type="submit">Save Changes</button>
    </form>
  </main>
</body>
</html>
