<!DOCTYPE html>
<html lang="en">
<head>
  <meta charset="UTF-8">
  <title><%= menuItem.name %> Details</title>
  <link rel="stylesheet" href="/stylesheet/style.css">
</head>
<body>
  <%- include('../partials/_navbar.ejs') %>

  <main class="menu-details">
    <h1><%= menuItem.name %></h1>

    <% if (menuItem.photo) { %>
      <img src="<%= menuItem.photo %>" alt="<%= menuItem.name %>">
    <% } %>

    <p><strong>Price:</strong> $<%= menuItem.price %></p>

    <% if (user && user.role === 'admin') { %>
      <a href="/users/<%= selectedUser._id %>/menu/<%= menuItem._id %>/edit">✏️ Edit</a>
      <form action="/users/<%= selectedUser._id %>/menu/<%= menuItem._id %>?_method=DELETE" method="POST">
        <button type="submit">🗑 Delete</button>
      </form>
    <% } %>

    <a href="/users/<%= selectedUser._id %>/menu">← Back to Menu</a>
  </main>
</body>
</html>

